{
  "name": "Motzi",
  "description": "Neighborhood bakery CSA ordering site",
  "buildpacks": [
    { "url": "https://github.com/jakeg/heroku-buildpack-bun" },
    { "url": "https://github.com/heroku/heroku-buildpack-activestorage-preview" },
    { "url": "heroku/ruby" }
  ],
  "addons": [
    {
      "plan": "heroku-postgresql:essential-0",
      "options": { "version": "17" }
    }
  ],
  "env": {
    "RAILS_ENV":                { "value": "production" },
    "RACK_ENV":                 { "value": "production" },
    "RAILS_SERVE_STATIC_FILES": { "value": "enabled" },
    "SECRET_KEY_BASE":          { "generator": "secret" },
    "AWS_ACCESS_KEY_ID":        { "required": false },
    "AWS_SECRET_ACCESS_KEY":    { "required": false },
    "HASHID_SALT":              { "required": false },
    "STRIPE_PUBLISHABLE_KEY":   { "value": "pk_test_uAmNwPrPVkEoywEZYTE66AnV00mGp7H2Ud" },
    "STRIPE_SECRET_KEY":        { "required": false },
    "REVIEW_APP":               { "value": "true" },
    "SENDGRID_USERNAME":        { "required": false },
    "SENDGRID_PASSWORD":        { "required": false },
    "ANTHROPIC_API_KEY":        { "required": false },
    "GITHUB_TOKEN":             { "required": false },
    "TURNSTILE_SITE_KEY":       { "required": false },
    "TURNSTILE_SECRET_KEY":     { "required": false }
  },
  "formation": {
    "web":    { "quantity": 1, "size": "basic" },
    "worker": { "quantity": 1, "size": "basic" }
  },
  "scripts": {
    "postdeploy": "bin/rails db:migrate solid_queue:bootstrap db:seed"
  }
}

#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_SETUP:-}" != "1" ]]; then
  SKIP_LOG_CLEAR=1 SKIP_APP_RESTART=1 bin/setup
fi

if command -v mise >/dev/null 2>&1; then
  exec mise exec -- bundle exec foreman start -f Procfile.dev
fi

exec bundle exec foreman start -f Procfile.dev

default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: motzi
  host: localhost

test:
  <<: *default
  # TEST_DATABASE lets each git worktree run tests against its own database —
  # concurrent suites sharing motzi_test clobber each other's fixtures.
  database: <%= ENV.fetch("TEST_DATABASE", "motzi_test") %>
  host: localhost

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
  variables:
    # Postgres 17+: abort any transaction open longer than this, so a stuck
    # request or job can't pin one of essential-1's 20 connections (shared with
    # Solid Queue/Cable/Cache). The release phase sets 0 (disabled) for migrations.
    transaction_timeout: <%= ENV.fetch("PG_TRANSACTION_TIMEOUT", "60s") %>

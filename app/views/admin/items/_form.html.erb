<%= semantic_form_for [:admin, @item], builder: ActiveAdmin::FormBuilder do |f| %>
  <%= f.inputs :name, :description, :credits, :price %>
  <%= f.input :image, as: :file %>
  <%= f.actions %>
<% end %>

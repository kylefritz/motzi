<h2>Please order by midnight on <%= Setting.deadline_day %> for <%= @menu.name %>!!</h2>

<p><%= link_to "This week's menu!", current_menu_url(uid: @user.hashid) %></p>

<%= render partial: 'menu_mailer/credits_remaining' %>
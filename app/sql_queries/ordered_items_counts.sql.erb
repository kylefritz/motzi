select order_items.pickup_day_id, order_items.item_id, sum(quantity)
from orders
  join order_items on order_items.order_id = orders.id

where orders.menu_id = <%= @menu_id %>

group by pickup_day_id, order_items.item_id
